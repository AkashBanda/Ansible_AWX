- hosts: all
  tasks:
    - yum:
        name: "httpd"
        state: present
    - service:
        name: "httpd"
        state: started
    - copy:
        content: "This is Full stack CICD pipeline"
        dest: "/var/www/html/index.html"
